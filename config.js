// config.js - No API keys needed!
// This app now uses free APIs that don't require API keys:
// - OpenStreetMap Nominatim for geocoding
// - Open-Meteo for weather data
// - REST Countries for country information

window.CONFIG = {
  // No sensitive data - everything is handled by free, public APIs
  APP_NAME: 'Country Weather App',
  VERSION: '2.0.0'
};
